describe("Describe Name", () => {
    it("TEST for TRUE", () => 
        expect(true).toEqual(true)
    );

    it("TEST for null & undefiend",
    () =>
        expect(null)
        .toEqual(undefined)
    );
});